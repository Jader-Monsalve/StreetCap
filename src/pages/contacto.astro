---
import Layout from '../layouts/Layout.astro';
---

<Layout
  title="Contacto - StreetCap"
  description="¿Preguntas o sugerencias? Contáctanos ahora."
>
  <section class="pt-32 pb-20 bg-black min-h-screen">
    <div class="max-w-2xl mx-auto px-6">
      <h1 class="text-6xl font-black text-white mb-4">
        Contacto
      </h1>
      <p class="text-gray-400 text-lg mb-12">
        ¿Preguntas, sugerencias o partnership? Nos encantaría escucharte.
      </p>

      <form action="https://formspree.io/f/YOUR_FORM_ID" method="POST" class="space-y-6">
        {/* Name */}
        <div class="space-y-2">
          <label class="block text-sm font-medium text-gray-300">Nombre</label>
          <input
            type="text"
            name="name"
            placeholder="Tu nombre"
            class="w-full px-4 py-3 bg-gray-800/50 border border-gray-700 rounded-lg text-white placeholder-gray-500 focus:border-cyan-500 focus:outline-none transition"
            required
          />
        </div>

        {/* Email */}
        <div class="space-y-2">
          <label class="block text-sm font-medium text-gray-300">Email</label>
          <input
            type="email"
            name="email"
            placeholder="tu@email.com"
            class="w-full px-4 py-3 bg-gray-800/50 border border-gray-700 rounded-lg text-white placeholder-gray-500 focus:border-cyan-500 focus:outline-none transition"
            required
          />
        </div>

        {/* Subject */}
        <div class="space-y-2">
          <label class="block text-sm font-medium text-gray-300">Asunto</label>
          <select name="subject" class="w-full px-4 py-3 bg-gray-800/50 border border-gray-700 rounded-lg text-white focus:border-cyan-500 focus:outline-none transition">
            <option>Pregunta general</option>
            <option>Reporte de bug</option>
            <option>Sugerencia de feature</option>
            <option>Partnership</option>
            <option>Otro</option>
          </select>
        </div>

        {/* Message */}
        <div class="space-y-2">
          <label class="block text-sm font-medium text-gray-300">Mensaje</label>
          <textarea
            name="message"
            placeholder="Cuéntanos lo que piensas..."
            rows={6}
            class="w-full px-4 py-3 bg-gray-800/50 border border-gray-700 rounded-lg text-white placeholder-gray-500 focus:border-cyan-500 focus:outline-none transition resize-none"
            required
          ></textarea>
        </div>

        {/* Submit */}
        <button
          type="submit"
          class="w-full px-6 py-4 bg-gradient-to-r from-cyan-500 to-blue-600 text-white font-bold rounded-lg hover:shadow-lg hover:shadow-cyan-500/50 transition transform hover:scale-105"
        >
          Enviar mensaje
        </button>
      </form>

      {/* Contact Info */}
      <div class="mt-16 grid grid-cols-1 md:grid-cols-3 gap-6">
        <div class="bg-gradient-to-br from-gray-900/50 to-black border border-cyan-500/20 rounded-2xl p-6 text-center space-y-2">
          <div class="text-3xl">📧</div>
          <h3 class="font-bold text-white">Email</h3>
          <p class="text-gray-400 text-sm">
            <a href="mailto:hola@streetcap.com" class="hover:text-cyan-400 transition">
              hola@streetcap.com
            </a>
          </p>
        </div>

        <div class="bg-gradient-to-br from-gray-900/50 to-black border border-cyan-500/20 rounded-2xl p-6 text-center space-y-2">
          <div class="text-3xl">🐦</div>
          <h3 class="font-bold text-white">Twitter / X</h3>
          <p class="text-gray-400 text-sm">
            <a href="https://twitter.com/streetcap" class="hover:text-cyan-400 transition">
              @streetcap
            </a>
          </p>
        </div>

        <div class="bg-gradient-to-br from-gray-900/50 to-black border border-cyan-500/20 rounded-2xl p-6 text-center space-y-2">
          <div class="text-3xl">💬</div>
          <h3 class="font-bold text-white">Discord</h3>
          <p class="text-gray-400 text-sm">
            <a href="https://discord.gg/streetcap" class="hover:text-cyan-400 transition">
              Únete al servidor
            </a>
          </p>
        </div>
      </div>
    </div>
  </section>
</Layout>
