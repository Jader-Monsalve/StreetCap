---
const capModels = [
  {
    id: 1,
    name: 'Gorra Beisbolera',
    icon: '⚾',
    description: 'Clásica y versátil',
    image: '🧢',
  },
  {
    id: 2,
    name: 'Gorra de Malla',
    icon: '🎯',
    description: 'Ventilada y moderna',
    image: '🧢',
  },
  {
    id: 3,
    name: 'Gorra Snapback',
    icon: '🧃',
    description: 'Ajuste con botones',
    image: '🧢',
  },
  {
    id: 4,
    name: 'Visera Abierta',
    icon: '☀️',
    description: 'Sin corona',
    image: '👒',
  },
  {
    id: 5,
    name: 'Gorra Plana',
    icon: '🏖️',
    description: 'Estilo street',
    image: '🧢',
  },
];
---

<section class="relative w-full py-20 bg-black overflow-hidden">
  <!-- Background elements -->
  <div class="absolute top-0 left-0 w-96 h-96 bg-blue-500/5 rounded-full blur-3xl"></div>
  <div class="absolute bottom-0 right-0 w-96 h-96 bg-cyan-500/5 rounded-full blur-3xl"></div>

  <div class="relative z-10 max-w-7xl mx-auto px-6">
    <!-- Header -->
    <div class="mb-16 space-y-4">
      <h2 class="text-5xl md:text-6xl font-black text-white">
        Modelos de Gorras
      </h2>
      <p class="text-gray-400 text-lg max-w-2xl">
        Elige entre nuestros 5 modelos exclusivos. Cada uno con características únicas y personalizables.
      </p>
    </div>

    <!-- Models Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-6">
      {capModels.map((model) => (
        <a
          href={`/diseña?modelo=${model.id}`}
          class="group relative h-80 bg-gradient-to-br from-gray-900/50 to-black border border-cyan-500/20 rounded-2xl p-6 overflow-hidden hover:border-cyan-500/60 transition-all duration-300 cursor-pointer flex flex-col items-center justify-center hover:shadow-lg hover:shadow-cyan-500/20"
        >
          {/* Background glow on hover */}
          <div class="absolute inset-0 bg-gradient-to-br from-cyan-500/10 to-transparent opacity-0 group-hover:opacity-100 transition duration-300"></div>

          {/* Content */}
          <div class="relative z-10 text-center space-y-4">
            {/* Icon */}
            <div class="text-6xl animate-bounce group-hover:scale-110 transition">
              {model.icon}
            </div>

            {/* Name */}
            <h3 class="text-xl font-bold text-white group-hover:text-cyan-400 transition">
              {model.name}
            </h3>

            {/* Description */}
            <p class="text-sm text-gray-400 group-hover:text-gray-300 transition">
              {model.description}
            </p>

            {/* CTA */}
            <div class="pt-2">
              <span class="inline-block px-4 py-2 bg-cyan-500/10 text-cyan-400 text-sm font-medium rounded-lg group-hover:bg-cyan-500/20 transition">
                Personalizar →
              </span>
            </div>
          </div>

          {/* Border animation */}
          <div class="absolute inset-0 rounded-2xl border border-transparent bg-gradient-to-r from-cyan-500 to-blue-500 opacity-0 group-hover:opacity-10 transition duration-300 pointer-events-none"></div>
        </a>
      ))}
    </div>

    <!-- Additional info -->
    <div class="mt-16 p-6 bg-gradient-to-r from-cyan-500/10 to-blue-500/10 border border-cyan-500/20 rounded-2xl">
      <p class="text-gray-300 text-center">
        💡 Cada modelo puede personalizarse con colores, texturas y diseños únicos. ¡Elige uno y comienza tu creación!
      </p>
    </div>
  </div>
</section>
