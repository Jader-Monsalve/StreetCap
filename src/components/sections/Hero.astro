---
import CapViewer3D from '../CapViewer3D.astro';
---

<section class="relative min-h-screen w-full overflow-hidden bg-black flex items-center justify-center pt-20">
  <!-- Animated background gradient -->
  <div class="absolute inset-0 overflow-hidden">
    <div class="absolute top-0 left-1/4 w-96 h-96 bg-blue-500/20 rounded-full blur-3xl animate-pulse"></div>
    <div class="absolute top-1/3 right-1/4 w-96 h-96 bg-cyan-500/20 rounded-full blur-3xl animate-pulse" style="animation-delay: 1s;"></div>
    <div class="absolute bottom-0 left-1/2 w-96 h-96 bg-purple-500/10 rounded-full blur-3xl animate-pulse" style="animation-delay: 2s;"></div>
  </div>

  <!-- Content -->
  <div class="relative z-10 max-w-6xl mx-auto px-6 py-20 flex flex-col lg:flex-row items-center justify-between gap-12">
    <!-- Left side - Text -->
    <div class="flex-1 space-y-8">
      <div class="space-y-4">
        <h1 class="text-5xl md:text-7xl font-black leading-tight">
          <span class="text-white">Diseña tu</span>
          <br />
          <span class="bg-gradient-to-r from-cyan-400 via-blue-500 to-purple-600 bg-clip-text text-transparent">propia gorra</span>
          <span class="text-white ml-2">en 3D</span>
          <span class="inline-block text-5xl md:text-6xl ml-2 animate-bounce">🎨</span>
        </h1>

        <p class="text-gray-300 text-lg md:text-xl leading-relaxed max-w-xl">
          Elige un modelo, combina colores, agrega tu logo o nombre y visualízalo en tiempo real.
        </p>
      </div>

      <!-- CTA Button -->
      <div class="flex flex-wrap gap-4 pt-4">
        <a
          href="/diseña"
          class="inline-block px-8 py-4 bg-gradient-to-r from-cyan-500 to-blue-600 text-white font-bold rounded-lg hover:shadow-lg hover:shadow-cyan-500/50 transform hover:scale-105 transition"
        >
          Comenzar a diseñar →
        </a>
        <a
          href="/galeria"
          class="inline-block px-8 py-4 border border-cyan-500/50 text-cyan-400 font-bold rounded-lg hover:bg-cyan-500/10 transition"
        >
          Ver galería
        </a>
      </div>
    </div>

    <!-- Right side - 3D Cap Preview -->
    <div class="flex-1 relative">
      <div style="width: 100%; height: 500px;" class="bg-black/50 border border-cyan-500/20 rounded-3xl overflow-hidden">
        <CapViewer3D capType="baseball" />
      </div>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 z-10 animate-bounce">
    <div class="flex flex-col items-center gap-2 text-gray-400">
      <span class="text-sm font-medium">Explorar modelos</span>
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
      </svg>
    </div>
  </div>
</section>

<style>
  @keyframes pulse-slow {
    0%, 100% {
      opacity: 0.5;
    }
    50% {
      opacity: 0.8;
    }
  }
</style>
